{
  "name": "<%= name %>",
  "description": "<%= description %>",
  "repository": {
    "url": "<%= username %>/<%= name %>",
    "type": "git"
  },
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "autod": "autod"<% if (test){ %>,
    "test": "cross-env NODE_ENV=test mocha -u bdd"<% } %><% if (watch){ %>,
    "dev": "nodemon index.js --config nodemon.json"<% } %>
  },
  "author": "<%= username %> <<%= email %>>",
  "license": "MIT",
  "dependencies": {<% if (cors){ %>
    "kcors": "^2.2.1",<% } %>
    "koa": "^2.2.0",
    "koa-bodyparser": "^4.2.0",
    "koa-compose": "^4.0.0",
    "koa-logger": "^3.0.0",
    "koa-onerror": "^3.1.0",
    "koa-router": "^7.2.0"<% if (session){ %>,
    "koa-session": "^5.0.0"<% } %><% if (!simple){ %>,
    "koa-static": "^3.0.0",
    "koa-views": "^6.0.2",
    "pug": "^2.0.0-rc.1"<% } %>
  },
  "devDependencies": {
    "autod": "^2.8.0"<% if (watch){ %>,
    "nodemon": "^1.11.0"<% } %><% if (test){ %>,
    "cross-env": "^5.0.0",
    "mocha": "^3.4.2",
    "power-assert": "^1.4.2",
    "supertest": "^3.0.0"<% } %>
  }
}
